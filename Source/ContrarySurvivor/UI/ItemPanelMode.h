// Fill out your copyright notice in the Description page of Project Settings.

#pragma once

#include "CoreMinimal.h"
#include "ItemPanelMode.generated.h"

/**
 * Режим правой панели предметов (ADR-082, ТЗ издателя 29.08.2026: «не "экран обыска" и
 * "экран торговли" как два отдельных куска, а один экран из двух панелей, у которого меняется
 * режим»). UInventoryScreenWidget — это всегда одно и то же окно рюкзака; режим решает, что
 * делает клик по плитке предмета (HandleTileUse) и какое окно-напарник стоит слева (обыск/
 * магазин/ящик). Новых виджетов режимы не заводят — только развилку в существующем коде.
 *
 * Stash (личный ящик) — ЗАДЕЛ на будущее (ADR-082, этап 1): значение перечня заведено уже
 * сейчас, но сама механика личного ящика пока НЕ реализована.
 */
UENUM(BlueprintType)
enum class EItemPanelMode : uint8
{
	Normal UMETA(DisplayName = "Обычный рюкзак"), // клик по плитке — использовать/надеть/взять в руки, как раньше
	Search UMETA(DisplayName = "Обыск"),          // клик по плитке — переложить предмет в обыскиваемый контейнер
	Trade  UMETA(DisplayName = "Торговля"),       // клик по плитке — продать (реализация — этап 3 ADR-082)
	Stash  UMETA(DisplayName = "Личный ящик")     // задел: значение есть, механики пока нет
};
